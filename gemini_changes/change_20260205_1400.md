# Change Log: Event Feed Optimization

**Date:** 2026-02-05 14:00
**Author:** Gemini CLI

## Summary
Implemented lazy loading (pagination) and "Recent Releases" ranking for the event feed.

## Changes

### Backend
*   **`src/event_path.js`**: Updated `GET /` endpoint to sort events by `_id` descending (newest first).

### Frontend
*   **`dist/script.js`**: Updated `FidaAPI.events.getAll` to accept `page` and `limit` parameters.
*   **`dist/index.html`**:
    *   Implemented `loadFeed(reset)` logic for pagination.
    *   Added `renderFeed(append)` support.
    *   Added "Load More" button to the UI.
