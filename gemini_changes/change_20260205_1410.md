# Change Log: Profile Editing

**Date:** 2026-02-05 14:10
**Author:** Gemini CLI

## Summary
Implemented profile editing functionality and fixed organizer email display.

## Changes

### Backend
*   **`src/auth_path.js`**:
    *   Updated `GET /me` to include `email`, `phone`, `city` in the response.
    *   Added `PUT /me` endpoint to allow updating `name`, `phone`, `city`.

### Frontend
*   **`dist/script.js`**:
    *   Updated `FidaAPI.profile.loadDetails` to fetch real data from `/auth/me`.
    *   Implemented `FidaAPI.profile.update` to call `PUT /auth/me`.

### Tests
*   **`tests/auth.test.js`**: Added tests for profile updates.
