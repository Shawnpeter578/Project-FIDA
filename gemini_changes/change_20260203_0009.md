# Change Log - 2026-02-03 00:09

## Frontend Integration
*   **File:** `dist/login.html`
    *   Replaced `localStorage` auth logic with `fetch` calls to `/api/auth/signup` and `/login`.
    *   Added token storage and role-based redirection.
*   **File:** `dist/organizer.html`
    *   Added token check on load.
    *   Updated `fetchEvents` to pull data from `/api/events`.
    *   Updated `handleCreate` to upload events via `POST /api/events` (supporting FormData).
    *   Updated `onScan` to verify QR codes via `POST /api/events/checkin`.
*   **File:** `dist/index.html`
    *   Removed embedded `#authScreen` markup and logic.
    *   Added token check and redirect to `/login.html` if unauthenticated.
    *   Updated `fetchEvents` to pull from `/api/events`.
    *   Updated `toggleJoin` to use `POST /api/events/join`.
