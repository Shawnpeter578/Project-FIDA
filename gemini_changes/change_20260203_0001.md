# Change Log - 2026-02-03 00:01

## Authentication Update
*   **File:** `src/auth/middleware.js`
*   **Changes:**
    *   Updated `signTokenJWT` to accept and include `role` in the token payload.
    *   Updated `authenticateJWT` to extract `role` from the token and attach it to `req.userRole`. Defaults to `'user'` if missing.
